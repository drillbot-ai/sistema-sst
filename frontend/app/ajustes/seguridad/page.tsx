"use client";

import ListShell from "../../../components/ListShell";

export default function SeguridadPage() {
  return (
    <ListShell title="Seguridad" subtitle="Usuarios (temporalmente desactivado), roles y permisos por módulo.">
      <div className="card p-6 text-gray-600">En desarrollo.</div>
    </ListShell>
  );
}
